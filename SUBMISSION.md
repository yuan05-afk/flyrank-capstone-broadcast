# Capstone submission

Copy these fields into the FlyRank **Add submission** form.

## Deliverable links

https://github.com/yuan05-afk/flyrank-capstone-broadcast

## Notes

Next.js 14 + TypeScript + Prisma/SQLite Capstone (Broadcast). Clone: `git clone https://github.com/yuan05-afk/flyrank-capstone-broadcast.git`. Run: `pnpm install && pnpm db:push && pnpm db:seed`, then `pnpm dev:fake` and `pnpm dev`. Sign in with `broadcast_demo_key_001`. Tests: `pnpm test` (fake platform on :4100 required).

## Files

Leave empty - the public repo link covers completion credit.

## Checklist before paste

- [x] Repo is public
- [x] README renders with screenshots
- [x] Tests pass / demo runs from README
- [x] `SUBMISSION.md` matches the live remote URL
